var arr =['a','b','c','d','e']

// arr.push('e')
// arr.pop()  // 删除数组最后面的一个元素
// arr.unshift('qianmian')
// arr.shift() // 删除数组最前面的一个元素

// arr.include('a')   // 判断数组中是否有这个元素，有则返回true，没有则返回false
// arr.indexOf('a')  // 返回对应数组元素下标
// arr.lastIndexOf('a') //返回对应数组下标 不过是从后往前找，数组下标不变

// arr.splice(2,0,5)  // 在数组下标为2的位置插入5 ，0没有意义但是必须要写，不写就是删除
// arr.splice(2,3)  // 从下标2开始删除3个元素

// arr.forEach(function(item, index,arr) {  // item拿到对应数组某个位置的值，index 为这个位置的下标，arr为拿到这个数组
//   console.log(item)
//   console.log(index)
//   console.log(arr)
// })

// var newArr = arr.map(function(item,index,arr){
//   return item + '1'
// })

// console.log(newArr)


// var s = arr.join('-');   使数组arr变为a-b-c-d-e字符串 join中表示用什么连接
// console.log(s)

// var newArr = arr.reverse()  // reverse反转数组
// console.log(newArr)


var arr1 =[1,2,4,5,2,3,7,8]
// console.log(arr1.sort())

// arr1.sort(function(a,b){  // 回调函数 
//   return b-a
// })

// var newArr = arr.concat(arr1) // concat连接两个数组

console.log(newArr)